import React from 'react'

const TrainingCalendarGraph = ({ activities, selected }) => {
  // const data = activities.map((day, index) => ({
  //     y: day[selected],
  //     x: index
  //   }))
  return <div></div>
}

export default TrainingCalendarGraph
